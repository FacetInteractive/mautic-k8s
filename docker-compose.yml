version: '2'

services:
    db:
        image: mysql:5.7
        volumes:
          - /mautic-db-init:/docker-entrypoint-initdb.d
          - /mautic-db-data:/var/lib/mysql
        environment:
            MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
            MYSQL_DATABASE: ${MYSQL_DATABASE}
            MYSQL_USER: ${MYSQL_USER}
            MYSQL_PASSWORD: ${MYSQL_PASSWORD}
        networks:
          - mautic
        labels:
          - traefik.enable=false

    php:
        build: php7-fpm
        volumes:
            - ./mautic:/var/www/symfony
        environment:
            DB_HOST: db
            DB_USER: ${MYSQL_USER}
            DB_PASSWORD: ${MYSQL_PASSWORD}
            DB_NAME: ${MYSQL_DATABASE}
            DB_PORT: 3306
        networks:
          - mautic
        labels:
          - traefik.enable=false

    nginx:
        build: nginx
        volumes_from:
            - php
        volumes:
            - ./logs/nginx/:/var/log/nginx
        labels:
          - traefik.backend=mautic
          - traefik.frontend.rule=Host:mautic.druplug.com
          - traefik.docker.network=proxy
        networks:
          - mautic
          - proxy

    pma:
      image: phpmyadmin/phpmyadmin
      environment:
        PMA_HOST: db
        PMA_USER: ${MYSQL_USER}
        PMA_PASSWORD: ${MYSQL_PASSWORD}
        PHP_UPLOAD_MAX_FILESIZE: 1G
        PHP_MAX_INPUT_VARS: 1G
      labels:
          - traefik.backend=pma
          - traefik.port=80
          - traefik.frontend.rule=Host:pma-mautic.druplug.com
          - traefik.docker.network=proxy
      networks:
          - mautic
          - proxy

networks:
  proxy:
    external: true
  mautic:
    external: false
